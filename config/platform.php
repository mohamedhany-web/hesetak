<?php

return [
    /*
    |--------------------------------------------------------------------------
    | إحصائيات الصفحة الرئيسية
    |--------------------------------------------------------------------------
    */
    'homepage_stats' => [
        /** الحد الأدنى لعرض عدد الطلاب — 0 = عرض العدد الحقيقي فقط */
        'learners_min' => (int) env('HOME_STATS_LEARNERS_MIN', 0),
        /** إظهار علامة + بجانب العدد عند الوصول للحد الأدنى */
        'learners_show_plus' => env('HOME_STATS_LEARNERS_SHOW_PLUS', false),
    ],

    /*
    |--------------------------------------------------------------------------
    | توقيت الأكاديمية (إدخال المواعيد من المعلمين/الإدارة)
    | التخزين في قاعدة البيانات يبقى UTC عبر config('app.timezone').
    |--------------------------------------------------------------------------
    */
    'academy_timezone' => env('ACADEMY_TIMEZONE', 'Africa/Cairo'),

    /*
    |--------------------------------------------------------------------------
    | Twitter / X handle for public SEO meta (optional)
    |--------------------------------------------------------------------------
    */
    'twitter_handle' => env('PLATFORM_TWITTER_HANDLE', '@hesetak'),
];
